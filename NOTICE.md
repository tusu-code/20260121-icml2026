## Notes

- This repo contains **demo assets only** (no model weights).
- The overlay GIF renders a **single automatically selected mask** (picked to be the most "single-object" among all predicted masks).
